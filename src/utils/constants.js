const page = document.querySelector('.page');
const header = page.querySelector('.header');
const buttonWarning = page.getElementById('button_type_warning');

console.log(page);


export { page, header, buttonWarning }
